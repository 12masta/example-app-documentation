# Frontend test seams

Unit tests run with Jest and Testing Library (`npm test` / `yarn test`). Page modules under `src/pages/` are the usual place for UI behavior tests. Generated Orval clients are out of coverage. Do not start Playwright against this frontend from the Agent Hub repo.
