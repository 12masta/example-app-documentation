# Frontend purpose

`example-app-frontend` is a private RealWorld / Conduit SPA. People sign in, read a feed, open articles, write posts, follow authors, favorite posts, and edit their settings.

It is a demo product for Agent Hub QA Brief, not a production publishing platform. Behavior that differs from public RealWorld samples is documented under `product/` and should be treated as example-app policy.
