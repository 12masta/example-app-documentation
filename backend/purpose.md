# Backend purpose

`example-app-backend` is the ASP.NET API for the same Conduit / RealWorld demo. It stores users, articles, comments, follows, and favorites. The frontend is the primary client. QA Brief may clone this repo as the run target when the change under review is a backend PR.
